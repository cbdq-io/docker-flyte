---
# Config file for Trivy.
exit-code: 1

format: table

ignore-unfixed: true

scan:
  skip-files:
    - /home/flyte/.local/**/*.pyc

severity:
  - CRITICAL
  - HIGH

scanners:
  - vuln

pkg:
  type:
    - os
    - library

  ignore-unfixed: true
